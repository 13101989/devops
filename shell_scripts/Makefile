# use PHONY to expicitly tell Makefile tht the targets are not file names and to execute the commands found under these targets 
.PHONY: hello hello2

name = John

hello:
# using @ to not display also the command echo
	@echo "Hello World!"

hello2:
	@echo "Hello ${name}"